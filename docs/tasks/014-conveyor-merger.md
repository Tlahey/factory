# Feature: Conveyor merger

**Status:** Needs verification (partially implemented)

## Description

A conveyor merge system that combines multiple input conveyors into a single output conveyor.

## Requirements

- Support multiple input conveyors feeding into one output.
- Each input accepts any resource type.

## Notes

The `conveyor-merger` building folder already exists in `apps/game/src/game/buildings/`. Treat this task as verifying/completing the requirements above against the current implementation.
